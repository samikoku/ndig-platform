{
  "buildCommand": "npm install --legacy-peer-deps && vite build && esbuild server/_core/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
  "outputDirectory": "dist",
  "env": {
    "VITE_API_BASE": "http://localhost:3000",
    "NODE_ENV": "production"
  }
}
